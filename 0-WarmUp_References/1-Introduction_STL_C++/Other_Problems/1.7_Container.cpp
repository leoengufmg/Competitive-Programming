/*
Container (02:00:00)

sequential container
	sotres data in a particular sequence
	Position matters
	Ex: vector, list, forward_list, array
	
Associative container
	data is somehow associated with itself
	position does not matters
	key - Value
	Ex: map, multimap, set, multiset, unordered_map, unordered_set
	
Adapters containers
	sit on already existing containers and restrict their functionalities
	Are pure concepts
	Ex: stack, queue, priority_queue
Name: Leonardo R. Andrade
*/

#include <iostream>
#include <stack>

using namespace std;


int main()
{
	stack <int>  si;
	stack <char> sc;
}